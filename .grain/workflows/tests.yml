name: Tests

on:
  push:
    branches:
      - '**'
  pull_request:

jobs:
  test:
    runs-on: grain-sandbox
    steps:
    - name: Run tests
      run: |
        pip install pexpect
        python -m unittest discover -s tests
