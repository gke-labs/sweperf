apiVersion: v1
kind: Pod
metadata:
  name: {{NAME}}
  labels:
    app: swebench
spec:
  restartPolicy: Never
  containers:
  - name: swebench
    image: {{IMAGE}}
    env:
    - name: PAGER
      value: "cat"
    - name: GIT_PAGER
      value: "cat"
