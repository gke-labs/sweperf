apiVersion: agents.x-k8s.io/v1beta1
kind: Sandbox
metadata:
  name: manual-test-sandbox
spec:
  podTemplate:
    spec:
      containers:
      - name: main
        image: alpine
        command: ["sleep", "3600"]
      restartPolicy: Never
